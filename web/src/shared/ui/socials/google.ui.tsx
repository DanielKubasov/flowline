import {Button} from '../button.ui';

const Google = () => {
    return (
        <Button className='w-full' variant='outline' size='lg'>
            Google
        </Button>
    );
};

export {Google};
